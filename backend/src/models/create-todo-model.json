{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "todo",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "pattern": "^(?!\\s*$).+",
            "minItems": 1
        },
        "dueDate": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        }
    },
    "required": [
        "name",
        "dueDate"
    ],
    "additionalProperties": false
}